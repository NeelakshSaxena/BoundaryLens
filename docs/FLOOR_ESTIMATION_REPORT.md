# Phase 12: Floor Detection Report
_Generated by `scripts/12_estimate_floors.py` at 2026-09-10T03:45:45.032055+00:00._

## Sources
| status | meaning |
|---|---|
| `OBSERVED` | real OSM building:levels tag on the building |
| `PREDICTED` | supervised HistGradientBoosting / RandomForest on ~12.7k real OSM labels across greater Bengaluru, whole-cell spatial split, labelled ESTIMATED with calibrated confidence + range |
| `NOT_DETERMINABLE` | no real tag and the model abstains / OOD / vegetation-contaminated |

- CNN not used: no sub-metre / floor-resolving imagery for these buildings; a CNN cannot see floors in Sentinel-2 (10 m).
- Label source: **OpenStreetMap building:levels (ODbL) - crowd-sourced Tier-2**.
- Features: footprint area, perimeter, compactness, rectangularity, elongation, long/short side, vertex count, building type, name presence, OSM height tag, coarse Copernicus GLO-30 DSM height.
- Confidence: floor_confidence = calibrated empirical P(prediction within +/-1 floor), isotonic regression on a held-out validation set. Not a legal certainty.
- Acceptance: tiered on calibrated confidence + predicted range, in-distribution only: HIGH (>=accept_high, range<=+/-2) and MEDIUM (>=accept_medium, range<=+/-3) get floor-level IDs; LOW (>=accept_low, range<=+/-4) is shown for review with NO IDs; everything else NOT_DETERMINABLE.

## Model (see `docs/FLOOR_MODEL_REPORT.md` for the full comparison)
- chosen: **random_forest**; train **8441** / val **2327** / test **1969** (spatial split, greater Bengaluru, AOI excluded).
- held-out **test**: MAE **2.22** | RMSE 3.67 | exact 26% | within +/-1 **59%** | within +/-2 75%.
- independent AOI check (n=16, never trained): MAE 1.15, within +/-1 62%.
- tiered acceptance on calibrated P(within +/-1): HIGH >= 70% & range <= +/-2; MEDIUM >= 58% & range <= +/-3; LOW >= 45% & range <= +/-4 (shown for review, NO floor-level IDs); otherwise NOT_DETERMINABLE. Every ML tier is labelled ESTIMATED and carries confidence + an expected range. HIGH from footprint shape alone is rare - a real OSM tag is what usually reads HIGH.

## Results (all 2734 buildings)
- detection status: `{'NOT_DETERMINABLE': 276, 'PREDICTED': 2442, 'OBSERVED': 16}`
- source: `{'NONE': 276, 'ML_MODEL': 2442, 'REAL_OSM_BUILDING_LEVELS': 16}`
- confidence state: `{'NOT_DETERMINABLE': 276, 'LOW': 446, 'MEDIUM': 1968, 'HIGH': 44}`
- PREDICTED buildings: **2442**, mean calibrated confidence **0.7**
- human verification required: **2722 / 2734**
- out-of-distribution rejected: **0**
- proposed floor-level IDs generated: **5713**

### Floor-count distribution (OBSERVED + PREDICTED)
| floors | buildings |
|---:|---:|
| 1 | 2 |
| 2 | 484 |
| 3 | 1747 |
| 4 | 219 |
| 5 | 3 |
| 11 | 3 |
| _not determinable_ | 276 |

## Non-regression
`building_height_m`, `derived_floors`, `building_levels`, `ground_elevation_m`, NDVI fields, `match_status_2d`, `final_verification_status` are read-only here.

## Data integrity
- **No synthetic training, validation, prediction or demonstration data.** Every label is a real OSM `building:levels` tag; every feature comes from real footprint geometry or real OSM tags; confidence is a calibrated empirical probability. Proposed IDs are `PROPOSED_LINKAGE_NOT_OFFICIAL_ISSUANCE`, never an official ULPIN.
