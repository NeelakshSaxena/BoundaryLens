# Topology Validation Skill

Validate:
parcel ↔ building
building ↔ floor
floor ↔ unit

Check:
- containment
- intersection
- overlap
- gaps
- invalid geometry
- duplicate entities

Use deterministic computational geometry first.

Known-invalid fixtures must fail the validator.
